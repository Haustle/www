export default function Arrow(){
    // just predefining because it throws error in vscode
    const arrow = "->"
    return(
        // inter font formats all -> to fleshed arrows
        <span className="inter arrow">{arrow}</span>
    )
}